/**
 * Placeholder for future email integration. Called after OTP is generated.
 */
export async function sendPasswordResetOtp(_email: string, _otp: string): Promise<void> {
  // Intentionally empty — wire SES/SendGrid/etc. here later.
}
